<UserControl x:Class="ReferenceManager.Views.ListItems"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReferenceManager.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ListView ItemsSource="{Binding Items}">
            <ListView.View>
                <GridView local:GridViewColumnCollection.HeaderTextMember="HeaderText" 
                          local:GridViewColumnCollection.DisplayMemberMember="DisplayMember"
                        local:GridViewColumnCollection.ColumnsSource="{Binding ColumnDefinitions}">
                </GridView>
                <!--<GridView local:GridViewColumns.HeaderTextMember="HeaderText" 
                          local:GridViewColumns.DisplayMemberMember="DisplayMember"
                        local:GridViewColumns.ColumnsSource="{Binding Columns}">
                </GridView>-->
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
